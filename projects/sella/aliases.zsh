alias web='docker exec -it sella_web'    # run a command in the web docker container
alias db='docker exec -it sella_db'      # run a command in the db docker container
alias gcod='git checkout develop'
alias rc='web bin/rails c'
